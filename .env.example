# Same Supabase project as HM Dormitory (apex-tech-landing page).
# Copy to .env.local and fill in the two keys from that project.
# Never put the service role key in this frontend.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SITE_SLUG=njmrf-pharmacy
